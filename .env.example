# Copy this file to `.env` and adjust locally. `.env` is git-ignored and must never be committed.
# Both values are optional: playwright.config.ts falls back to the public defaults shown here.
BASE_URL=https://www.centraldepasajes.com.ar
API_BASE_URL=https://www.swapi.tech
